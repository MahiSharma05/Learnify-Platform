package com.learnify.enrollment.enums;

public enum EnrollmentStatus {

    ACTIVE,      // Student is actively enrolled and learning
    COMPLETED,   // Student has finished all lessons (100% progress)
    CANCELLED    // Student unenrolled or admin cancelled
}