package com.learnify.paymentservice.enums;

public enum SubscriptionPlan {
    FREE,     // Limited access — no payment required
    MONTHLY,  // Full access — billed every 30 days
    ANNUAL    // Full access — billed every 365 days (discounted)
}